# Backend example env - copy to backend/.env and edit values
PORT=4000
# If you want to use Firebase Admin on the server, set the service account path or provide a base64 JSON
# Place your service account JSON at backend/serviceAccount.json and set:
FIREBASE_SERVICE_ACCOUNT_PATH=./serviceAccount.json
# Or set the base64-encoded JSON directly (useful for CI):
# FIREBASE_SERVICE_ACCOUNT_BASE64=eyJ0eXAiOiJKV1QiLCJhbGciOiJ... (base64)

# MongoDB connection
MONGO_URI=mongodb://127.0.0.1:27017/innterbridge-dev
# JWT secret for backend-signed tokens (used as fallback)
JWT_SECRET=devsecret

# NOTE: The actual serviceAccount.json file contains sensitive credentials. Do NOT commit it to the repository.
