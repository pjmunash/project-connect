PORT=4000
MONGO_URI=mongodb+srv://pjkabaiya_db_user:orBfWMWpbjoPtvNa@cluster0.f40gykp.mongodb.net/innterbridge?retryWrites=true&w=majority&appName=Cluster0
JWT_SECRET=4r5yu67e3459ih213874t6y5u4r3e2w1q
OPENAI_API_KEY=your_openai_api_key_here
FIREBASE_SERVICE_ACCOUNT_PATH=./serviceAccount.json

# Backend environment for Innterbridge
# WARNING: Do NOT commit this file to source control. Keep secrets private.

NODE_ENV=development

# MongoDB Atlas connection. Replace the password if you rotate credentials.

# JWT secret used by backend-issued tokens (keep long & random)

# OpenAI key (optional, used for AI features)

# Firebase Admin service account. You can provide either a path to the
# JSON file (below) or set FIREBASE_SERVICE_ACCOUNT_BASE64 with the file
# base64-encoded (use one method only).
# FIREBASE_SERVICE_ACCOUNT_BASE64=
