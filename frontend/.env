# Frontend environment variables
# Enable mock auth for development (accepts any email with password 'admin')
VITE_MOCK_AUTH=false

# Backend API base (used when not mocking or for hybrid flows)
VITE_API_BASE=http://localhost:4000/api

VITE_USE_FIREBASE=true
